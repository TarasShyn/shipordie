import { ComponentType } from 'react';

import { CrewPersonData } from './types';

export declare const GameCanvas: ComponentType<{ people: CrewPersonData[] }>;
export declare const ShipPreview: ComponentType<Record<string, never>>;
